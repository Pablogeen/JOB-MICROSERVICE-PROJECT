version : '3.7'
services:
  mysql:
    container_name: mysql
    image: mysql
    environment:
      MYSQL_USER: root
      MYSQL_PASSWORD: root
    volumes:
      - mysql:/data/mysql
    ports:
      - "3307:3307"
    networks:
      - mysql
    restart: unless-stopped



